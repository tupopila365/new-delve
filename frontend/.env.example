# Local overrides: copy to .env.development.local or .env.local (do not commit secrets).

# Use real Django API (required for beta / Heroku).
VITE_USE_MOCKS=false

# Note: even with VITE_USE_MOCKS=true, /api/coin-toss/* always calls Django.
# Django API origin. Leave empty only when the API is same-origin (not typical on Heroku).
# Local: http://127.0.0.1:8000
# Heroku: https://delve-api-xxxx.herokuapp.com
VITE_API_URL=http://127.0.0.1:8000

# Delve Admin console (staff handoff from /admin).
# Local: http://localhost:5174
# Heroku: https://delve-admin-xxxx.herokuapp.com
VITE_ADMIN_APP_URL=http://localhost:5174

# Google Maps JavaScript API (Places search + traveler stay maps + provider venue picker).
# Put the real key in .env.development.local (gitignored), then restart Vite.
# In Google Cloud Console you must also:
#   1) Link a billing account (required even on the free tier)
#   2) Enable "Maps JavaScript API" and "Places API"
#   3) Key restrictions: HTTP referrers → http://localhost:5173/* and http://127.0.0.1:5173/*
VITE_GOOGLE_MAPS_API_KEY=
