# Local dev: most APIs use in-memory mocks (no Django required).
# Coin Toss always calls the real Django API at /api/coin-toss/ (proxied to :8000).
# To use the real backend for everything, create .env.development.local with:
#   VITE_USE_MOCKS=false
VITE_USE_MOCKS=true
